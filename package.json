{
    "name": "whisper",
    "private": true,
    "workspaces": [
        "packages/*",
        "platforms/*"
    ],
    "scripts": {
        "dev": "bun run --filter @whisper/core dev",
        "build": "tsc --build",
        "test": "vitest run",
        "typecheck": "tsc --build",
        "lint": "biome check .",
        "format": "biome format --write ."
    },
    "devDependencies": {
        "@biomejs/biome": "2.4.6",
        "@types/node": "^25.3.5",
        "typescript": "^5.7.0",
        "vitest": "^3.0.0"
    }
}
