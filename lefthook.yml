pre-commit:
  commands:
    format:
      glob: "*.{ts,tsx,js,jsx,json,css}"
      run: bunx @biomejs/biome format --write {staged_files}
      stage_fixed: true
    lint:
      glob: "*.{ts,tsx,js,jsx}"
      run: bunx @biomejs/biome check --fix {staged_files}
      stage_fixed: true
